- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.rbenv: .rbenv
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.bash_profile: .bash_profile
    ~/.bash_prompt: .bash_prompt
    ~/.bashrc: .bashrc
    ~/.profile: .profile
    ~/.screenrc: .screenrc
    ~/.gvimrc: .gvimrc
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim/
#    ~/.vimrc: vimrc

- shell:
  -
    - source ~/.bash_profile
    - get into new bash shell
  -
    - mkdir -p ~/Projects/archive ~/Projects/current
    - Creating Project directories directories
  -
    - git submodule update --init --recursive
    - Installing/updating submodules
  -
    - _update_hosts
    - replace /etc/hosts with http://someonewhocares.org/hosts/zero/hosts
